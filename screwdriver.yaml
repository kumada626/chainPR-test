annotations:
    screwdriver.cd/chainPR: true

jobs:
  first:
    requires: [ ~pr, ~commit ]
    image: node:12
    steps:
      - ls: ls
  second:
    requires: first
    image: node:12
    steps:
      - ls: ls
